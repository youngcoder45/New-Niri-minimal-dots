#!/bin/sh

CHOICE=$(printf "Lock\nLogout\nReboot\nShutdown\n" | \
    fuzzel --dmenu --prompt "Power > ")

case "$CHOICE" in
  "Lock")
    swaylock -f -c 000000
    ;;
  "Logout")
    niri msg quit
    ;;
  "Reboot")
    systemctl reboot
    ;;
  "Shutdown")
    systemctl poweroff
    ;;
esac

